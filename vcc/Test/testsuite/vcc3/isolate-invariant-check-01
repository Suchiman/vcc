`/newsyntax
#include <vcc.h>

struct A {
  int x;
};

struct B {
  struct A a;
  _(invariant \mine(&a))
};

void foo(struct B *b)
  _(updates b)
{
  _(unwrap b)
  _(unwrap &b->a)
  b->a.x = 3;
  _(wrap &b->a)
  _(wrap _(isolate) b)
}

void foo2(struct B *b)
  _(updates b)
{
  _(unwrapping _(isolate) b)
  _(unwrapping &b->a)
  b->a.x = 3;
}

`
Verification of B#adm succeeded.
Verification of foo succeeded.
Verification of foo2 succeeded.
`
