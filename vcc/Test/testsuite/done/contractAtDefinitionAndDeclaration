#include <vcc.h>
#include <sal.h>

_Bool f(int *x)
ensures(result == 0);

_Bool f(int *x)
ensures(1)
{ 
  return 1;
}

`
testcase(7,1) : warning VC9002: The declaration of function 'f' already specifies contracts. Discarding the contracts of the definition.
Verification of f failed.
testcase(10,3) : error VC9501: Post condition 'result == 0' did not verify.
testcase(5,11) : error VC9599: (related information) Location of post condition.
`

#include <vcc.h>

void foo(int a spec(int b))
  requires(a == b);
  
void foo(int a) {};
`
testcase(7,1) : error VC9658: declared formal parameter list different from definition
testcase(4,1)-(4,39) : (Location of symbol related to previous error.)
`
