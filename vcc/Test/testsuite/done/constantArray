#include <vcc.h>

unsigned const int a[] = {0, 1, 2, 3, 4, 5};

unsigned int foo(unsigned int n)
  requires(n < 6)
  ensures(result == n)
  requires(thread_local(&a[n]))
  reads(&a[n])
{
  return a[n];
}

`
Verification of foo succeeded.
`
#include <vcc.h>

unsigned const int a[] = {0, 1, 2, 3, 4, 5};

unsigned int foo(unsigned int n)
  requires(n < 6)
  ensures(result == n)
  reads(&a[n])
{
  return a[n];
}
`
Verification of foo succeeded.
`
#include <vcc.h>

unsigned const int a[] = {0, 1, 2, 3, 4, 5};

void fooShoudFail(unsigned int n)
  requires(n < 6)
  writes(&a[n])
{
  a[n] = 5;
}
`
testcase(9,3) : error VC0000: The left-hand side of an assignment must be a variable, property or indexer.
`
