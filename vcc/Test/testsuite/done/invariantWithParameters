#include <vcc2.h>

typedef unsigned __int64 UINT64, *PUINT64;

void foo(PUINT64 p, UINT64 c)
  reads(set_universe())
{
  UINT64 i;
  PUINT64 q;
  
  q = p;
  for (i = 0; i < c; i++)
    invariant(p == q)
  {
    p++;
    q++;
  }
}

`
Verification of foo succeeded.
`