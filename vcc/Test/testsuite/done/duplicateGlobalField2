#include <vcc.h>

extern int perfect;

static void f()
  writes(&perfect)
  requires(mutable(&perfect))
{
  perfect = 12;
}


int perfect = 4;

int foo()
  writes(&perfect)
  requires(mutable(&perfect))
{
  perfect =0;
  return 0;
}
`
Verification of f succeeded.
Verification of foo succeeded.
`
#include "vcc.h"

int a;

struct G {
    int a;
};

void foo(struct G *x) {
    assume(a==3);
}
`
Verification of foo succeeded.
`
