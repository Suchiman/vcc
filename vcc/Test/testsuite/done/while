#include <vcc.h>

void WhileTest(int n)
  requires(0<=n)
{ int i;
  i = 0;
  while(i<n)
    invariant(0<=i)
    invariant(i<=n)
  {
    i=i+1;
  }
   assert(i==n);
}
`
Verification of WhileTest succeeded.
`
#include <vcc.h>

int foo(){
  int i = 0;
  while (i >= 0 && i < 10){
    if (i < 20) ++i; else assert(0);
    if (i == 1) continue;
    if (i == 2) goto xx; // previous version triggered bogus smoke test error
  }
xx: return i;
}
`
Verification of foo succeeded.
`
