#include <vcc2.h>

void foo()
{
	int i;

	for(i=0; i<5; i++)
	{
		assert(typed(result));
	}
}
`
testcase(9,24) : error VC0000: The name 'result' does not exist in the current context.
`
