#include <vcc2.h>

extern void* malloc(unsigned int);

int main() {
  char* str = (char*) malloc(5);
  assert(str != 0);
  return 0;
}
`
Verification of main succeeded.
`
#include <vcc2.h>

extern void* malloc(unsigned int);

int main() {
  char* str = (char*) malloc(5);
  assert(str != 0);
  return 0;
}
`
Verification of main succeeded.
`