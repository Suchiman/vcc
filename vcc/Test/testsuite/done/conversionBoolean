// Casting various number to Boolean used to cause incorrect IL to be generated


int main (){
  
  short v1 =1;
  unsigned v2 =1;
  char v3 = 1;
  long v4 = 1;
  long long v5 =1;
  
  int count =6;

  if (v1) {count--; } else __assert(0);
  if (v2) {count--;} else __assert(0);
  if (v3) {count--;} else __assert(0);
  if (v4) {count--;} else __assert(0);
  if (v5) {count--;} else __assert(0);

  if (1) count--; else __assert(0);
  __assert(count == 0);
}
`
Verification of main succeeded.
`
// conversion from Boolean to other integral types.
int main()
{
   int i = -1;
   __assert( 1 || i);
	
}
`
Verification of main succeeded.
`