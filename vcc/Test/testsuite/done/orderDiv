#include <vcc.h>

typedef long long __vcI8;
typedef unsigned long long __vcU8;




void orderDivTestU(unsigned int a, unsigned int b)
  requires(0 < b)
{
  assert(a / b <= a);
}

`
Verification of orderDivTestU succeeded.
`
