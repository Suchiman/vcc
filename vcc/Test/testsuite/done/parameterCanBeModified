#include <vcc2.h>

void f(unsigned k, unsigned l) {
  k = 0;
  l = 0;
  while(k++ <= 10)
    invariant(0 <= k && k <= 11)
    invariant(l==k)
  {
    l++;
  }
  assert(l==11);
}

`
Verification of f succeeded.
`
