#include <vcc.h>

struct Core {
  unsigned __int64 a;
  unsigned __int64 b;
};

struct Outer {
  struct Core core;
};


ispure struct Outer foo();

void bar()
  requires(foo().core.a == 0) {
}

`
Verification of bar succeeded.
`
