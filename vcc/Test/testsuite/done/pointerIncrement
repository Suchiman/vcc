#include <vcc2.h>

typedef struct _PTE
{
  int a;
  int b;
} PTE, *PPTE;


void foo(PPTE p)
{
  PPTE q; 
  q = p;
  q++;
  q--;
  p++;
  p--;
  assert(p == q);
}
`
Verification of foo succeeded.
`