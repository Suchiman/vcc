#include <vcc.h>

struct x {
    spec(int Map2[int];)
    int a;
} z;

void foo() {
    struct x z;
    //x.a = 10;
    x.Map2[0] = 3;
}

`
testcase(11,5) : error VC0000: Illegal index.
testcase(11,5) : error VC0000: left of '.Map2' must have struct/union type
`
#include <vcc.h>

struct x {
    spec(int Map2[int];)
    int a;
} z;

void foo() {
    struct x z;
    x.a = 10;
}
`
testcase(10,5) : error VC0000: An object reference is required for the nonstatic field, method, or property 'a'.
testcase(5,5)-(5,10) : (Location of symbol related to previous error.)
`