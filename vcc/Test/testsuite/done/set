#include <vcc.h>

void foo()
{
  
  spec(ptrset a, b;)
  void *x;

  assert(set_in(x,a) && !set_in(x,b) ==> set_in(x,set_difference(a,b)));
  assert(set_in(x,a) && set_in(x,b) ==> !set_in(x,set_difference(a,b)));
  assert(!set_in(x,a) && set_in(x,b) ==> !set_in(x,set_difference(a,b)));
  assert(!set_in(x,a) && !set_in(x,b) ==> !set_in(x,set_difference(a,b)));
}
`
Verification of foo succeeded.
`