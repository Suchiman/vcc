#include <vcc2.h>

unsigned int ispure bar()
{
   return(5);
}

void foo(unsigned int x)
requires(x > 10)
{
    assert(bar() >= 0);
    return;
}

`
Verification of bar succeeded.
Verification of foo succeeded.
`