#include "vcc.h"

void foo(int a spec(out int n))
  ensures(n == a);

void bar() {
    spec(int n[unsigned] = lambda(unsigned i; 0);)
    foo(0 spec(out n[0]));
}
`
testcase(8,31) : error VC0000: A map access cannot be passed as an out parameter.
`
