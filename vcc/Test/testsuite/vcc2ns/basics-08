#include <vcc.h>

void bar(unsigned i) {
   spec(bool map[unsigned];) 
   spec(map = (lambda(unsigned j; true; (bool) ((i==j)? 1: (map[j]))));)
}
`
testcase(5,67) : error VC0000: Type of conditional expression cannot be determined because there are implicit conversions between '__int32' and 'bool'; try adding an explicit cast to one of the arguments.
`
