#include "vcc.h"

struct vcc(record) S {
    int a;
};

void foo()
{
    spec(struct S map2[int];)
    spec(map2 = lambda(int x; true; (struct S) { .a = x });)
    assert(map2[4].a == 4);
}
`
Verification of foo succeeded.
`
