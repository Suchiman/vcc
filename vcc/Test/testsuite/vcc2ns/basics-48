#include <vcc2test.h>

vcc(claimable) struct T
{
    struct S c;
};

void foo() {
  struct T *t = (struct T *)malloc(sizeof(struct T));
  assert(t == NULL || is_claimable(t));
}
`
Verification of foo succeeded.
`
