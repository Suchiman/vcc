`/newsyntax
#include <vcc.h>
_(ghost 
\natural plus(\natural x, \natural y)
  _(ensures \result == x + y)
{
  _(ghost \natural z = x+y;)
  while (x>(\natural)0)
    _(invariant x + y == z)
    {
      x-= (\natural)1;
      y+= (\natural)1;
    }
  return y;
}) 
`
Verification of plus succeeded.
`
