#include "vcc.h"

struct A {
  int y;
  spec( int m[int]; )
};

void foo(struct A *a)
  requires(thread_local(a))
{
  spec( a->m[12] = 11; )
}
`
Verification of foo failed.
testcase(11,20) : error VC8507: Assertion 'a->m is writable' did not verify.
`
