#include <vcc.h>

struct vcc(claimable) S {
  int x;
};


void foo(struct S *s)
  writes(s)
  maintains(wrapped0(s))
{
  expose(s) {
    s->x = 12;
  }
}
`
Verification of foo succeeded.
`
