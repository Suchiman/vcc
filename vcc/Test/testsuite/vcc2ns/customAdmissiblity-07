#include <vcc.h>

struct A {
  int x;
  invariant(this->x>0)
};


spec(isadmissibilitycheck
void adm_A(struct A* x);)

void qux()
{
  spec( adm_A((struct A*)0); )
}
`
testcase(9,17) : error VC9644: the admissibility check is required to have a body
`
