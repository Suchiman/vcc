#include "vcc.h"

typedef struct _FOO
{
	int dummy;
	spec(bool Map[AAA];)
	invariant(Map[dummy])
} FOO;


isadmissibilitycheck 
void funcname(FOO *p)
{ 
	havoc_others(p);
}

`
testcase(6,27) : error VC0000: The name 'AAA' does not exist in the current context.
`
