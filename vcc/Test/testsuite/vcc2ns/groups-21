#include <vcc.h>

struct A { int x; };

void foo(struct A *a)
{
spec( obj_t o = owner((struct B::C *)a); )
}
`
testcase(7,52) : error VC0000: The name 'B' does not exist in the current context.
`
