#include <vcc.h>
 
struct vcc(claimable) Foo
{
  invariant(true)
    int x;
};
 
void foo(struct Foo *o claimp(c))
  always(c,closed(o))
  writes(o)
  ensures(false)
{
  spec(claim_t c1;)

  spec(c1 = claim(o, false);)
  assert(false);
}
`
Verification of Foo#adm succeeded.
Verification of foo failed.
testcase(16,24) : error VC8520: Assertion 'chunk ((bool)0) of the claim initially holds' did not verify.
`
