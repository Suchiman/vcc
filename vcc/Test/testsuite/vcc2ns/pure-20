`/newsyntax
#include <vcc.h>
#include <stdlib.h>

struct S {
  int a;
};

struct S *foo() 
  _(ensures \result->\valid)
  _(ensures \thread_local(\result))
  _(ensures \fresh(\result))
  _(ensures \mutable(\result))
{
  struct S *p = (struct S *)malloc(sizeof(struct S));
  _(assume p != NULL)
  return p;
}

void bar() {
  struct S *p;
  _(wrap (p = foo()))
  _(unwrap (p = foo()))
}
`
testcase(21,25) : warning VC9300: [possible unsoundness]: assignment to physical location from specification code
testcase(22,27) : warning VC9300: [possible unsoundness]: assignment to physical location from specification code
testcase(21,29) : error VC9635: function 'foo' used in pure context, but not marked with 'ispure'
testcase(8,8)-(17,2) : (Location of symbol related to previous error.)
testcase(22,31) : error VC9635: function 'foo' used in pure context, but not marked with 'ispure'
testcase(8,8)-(17,2) : (Location of symbol related to previous error.)
`