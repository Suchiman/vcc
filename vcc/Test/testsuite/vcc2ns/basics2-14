#include "vcc.h"
#ifdef VERIFY

struct vcc(record) S {
    int a;
};

void foo()
{
    spec(struct S x;)
    spec(x = /* missing: (struct S) */ { .a = 3 };)
}

#endif
`
testcase(11,26) : error VC0000: Cannot determine initializer's type from context.
`
