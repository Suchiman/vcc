#include <vcc.h>

int foo(spec(out int o)) 
  ensures(o == 5)
  ensures(result == 6)
{
  spec(o = 5;)
  return 6;
}		

void bar() {
  spec(int p;)
  int r;
  r = foo(spec(out p));
  assert(p == 5);
  assert(r == 6);
}

`
Verification of foo succeeded.
Verification of bar succeeded.
`
