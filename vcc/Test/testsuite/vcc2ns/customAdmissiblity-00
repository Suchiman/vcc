#include <vcc.h>

struct A {
  int x;
  invariant(this->x>0)
};


spec(isadmissibilitycheck
void adm_A(struct A* x)
{
})
`
Verification of A#adm succeeded.
Verification of adm_A failed.
testcase(9,17) : error VC8002: state was altered after havoc_others().
`
