#include "vcc.h"

spec(
typedef struct S_str {
unsigned int id;
} S_t;
)

spec(
struct abs_str {
bool Sset[S_t];

invariant(Sset == lambda(S_t s; true ; s.id < 42))
} abs;
) 
`
testcase(4,9) : error VC9702: Illegal type 'struct S_str' in map domain.
`
