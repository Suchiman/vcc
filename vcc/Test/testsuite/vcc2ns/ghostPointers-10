
#include <vcc.h>

struct S {
  int a;
  spec(int b;)
  spec(int ^p;)
  invariant(p != &b)
};

struct T {
  int a;
  spec(int b;)
  spec(int ^p;)
  invariant(p == &b)
};
`
Verification of S#adm succeeded.
Verification of T#adm succeeded.
`
