#include "vcc.h"

void foo(spec(out int a)) {
  block 
    ensures(a == 10)
  {
    spec(a = 10;)
    assert(a == 10);
  }
  assert(a == 10);
}
`
Verification of foo succeeded.
Verification of foo#block#0 succeeded.
`
