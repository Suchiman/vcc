#include <vcc.h>

void foo(int * x spec(out int o))
  ensures(o == 5)
  out_param(x)
{
  spec(o = 5;)
}

void bar()
{
  int x;
  spec(int o;)

  foo(&x spec(out o));
}
`
Verification of foo succeeded.
Verification of bar succeeded.
`
