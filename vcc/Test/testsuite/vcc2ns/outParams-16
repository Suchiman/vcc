#include "vcc.h"

void foo(claimp(z) claimp(out x));

struct X { int dummy; spec(claim_t e;)};

void bar(struct X *x) {
    claimp(c;)
    foo(spec(c) spec(out x->f/* should be e*/));
}
`
testcase(9,48) : error VC0000: 'X' does not contain a definition for 'f'.
`
