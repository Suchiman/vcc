#include "vcc.h"

struct Foo {
    int a;
    spec(int a;)
};

void foo(struct Foo *s) {}
`
testcase(5,21) : error VC9677: 'a' : 'struct' member redefinition
testcase(4,5)-(4,10) : (Location of symbol related to previous error.)
`
