`/ps:32 /newsyntax


#include <vcc.h>

struct S {
  long *p;
  long i;
};

void foo(struct S *x) { 
  size_t s = sizeof(struct S);
  _(assert s == 8)
  _(assert (char *)(&x->i) == ((char *)x) + 4)
}
`
Verification of foo succeeded.
`
