#include "vcc.h"

struct A {
  int a;
  invariant(depends(owner(this), this))
};

void foo(struct A *a)
  requires(wrapped(a))
  writes(a)
{
unwrap(a);
a->a = 7;
wrap(a);
}
`
Verification of A#adm succeeded.
Verification of foo succeeded.
`
