`/a /functions:foo

#include <vcc.h>

template<typename T> void bar(T *p)
  writes(p);

struct X { int x; };

void foo(struct X *p) 
  writes(p)
{ 
  bar<struct X>(p); 
}
`
Verification of foo succeeded.
`
