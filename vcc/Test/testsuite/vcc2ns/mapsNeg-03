#include "vcc.h"

void bar(unsigned i) {
    spec(unsigned a[unsigned];)
    spec(a = lambda(unsigned j; true; i = 0);)
}
`
testcase(5,57) : error VC0000: Evaluating this expression has the side effect of modifying memory, which is not permitted in this context.
`
