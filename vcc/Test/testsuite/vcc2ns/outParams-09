
#include <vcc.h>

void foo(spec(out int i)) 
  ensures(i == 17)
{
  spec(i = 17;)
}

void bar(spec(int i)) {
  foo(spec(out i));
  assert(i == 17);
}
`
Verification of foo succeeded.
Verification of bar succeeded.
`
