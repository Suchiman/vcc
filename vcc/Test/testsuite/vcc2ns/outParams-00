#include <vcc.h>

void foo(spec(out int o)) 
  ensures(o == 5)
{
  spec(o = 5;)
}		

void bar() {
  spec(int p;)
  foo(spec(out p));
  assert(p == 5);
}

`
Verification of foo succeeded.
Verification of bar succeeded.
`
